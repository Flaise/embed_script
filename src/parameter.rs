
// #[cfg(test)]
// mod tests {
//     use combine::parser::char::letter;
//     use combine::parser::range::take_while1;
//     use combine::{many1, Parser};
//     // use combine::parser::byte::letter;

//     #[test]
//     fn variables() {
//         // let parser = many1(letter());
//         // assert_eq!(parser.parse("omg"), Ok(("omg", "")));

//         let parser = take_while1(letter());
//         assert_eq!(parser.parse("omg"), Ok(("omg", "")));

//     }
// }

#[cfg(test)]
mod tests {

    #[test]
    fn variables() {
        // let parser = many1(letter());
        // assert_eq!(parser.parse("omg"), Ok(("omg", "")));

        // let parser = take_while1(letter());
        // assert_eq!(parser.parse("omg"), Ok(("omg", "")));

        // many1

    }
}
